import React from "react";

import '../App.css';

function Subsubtitle(props) {
    return (
        <div className="subsubtitle-text">
            {props.text}
        </div>
    );
}

export default Subsubtitle;